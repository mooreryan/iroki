/**
 * The top-level namespace/module for Iroki code.
 *
 * Eventually, I'd like to move all the JS code into this namespace.  But for now, I will just focus on getting new stuff and refactorings into it.
 */
var IROKI = (function (iroki) {

  return iroki;
}(IROKI || {}));