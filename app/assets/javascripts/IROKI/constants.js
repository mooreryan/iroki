// These are tested but no specs.

var IROKI = (function (iroki) {
  /**
   * This module contains any constants used in Iroki.
   */
  iroki.constants = (function (constants) {
    constants.hide_leaf_label = "__IROKI_BLANK__";

    return constants;
  }(iroki.constants || {}));

  return iroki;
}(IROKI || {}));
